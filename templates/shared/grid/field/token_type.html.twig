{% set label = ('guiziweb_sylius_token.ui.transaction.types.' ~ data.value)|trans %}
 {% set badge = { 'credit': 'bg-green-lt', 'debit': 'bg-blue-lt', 'expiration': 'bg-orange-lt' }[data.value]|default('bg-blue-lt') %}

<span class="badge {{ badge }}">{{ label }}</span>