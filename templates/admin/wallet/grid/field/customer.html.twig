{% set customer = data.customer %}
{% if customer is not null %}
    <a href="{{ path('sylius_admin_customer_show', { id: customer.id }) }}">{{ customer.email }}</a>
{% else %}
    <span class="text-muted">—</span>
{% endif %}
