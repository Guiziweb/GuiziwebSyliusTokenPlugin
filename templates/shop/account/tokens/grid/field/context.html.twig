{% if data.order is not null %}
    <a href="{{ path('sylius_shop_account_order_show', { number: data.order.number }) }}">#{{ data.order.number }}</a>
{% elseif data.reason is not null %}
    {{ data.reason }}
{% else %}
    <span class="text-muted">—</span>
{% endif %}
